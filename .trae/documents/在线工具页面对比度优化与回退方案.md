## 目标
- 回退首页“分类区块背景”改动，保持原有视觉风格不变
- 专注优化“在线工具”各页面内的功能卡片与背景的对比度，提高清晰度和可读性

## 回退范围
- 恢复 `css/style-new.css` 中 `.categories-section` 背景为原设计（不使用深色梯度）
- 保留此前功能扩展（矩阵计算器、1D杆、形函数、Gauss 积分）与页面结构不变

## 工具页面对比度优化（不改信息架构）
- 页面背景：为各工具页主要内容区添加高对比深色背景（仅工具页），如 `main-content-section tool-contrast-dark`
- 功能卡片：
  - `tool-card` 改为纯白背景 `#ffffff`、深色边框 `rgba(17,24,39,0.15)`、强阴影 `var(--shadow-xl)`
  - `result-card` 同步提升为纯白或浅灰背景，边框加深，标题与正文颜色提高对比
- 表单与图标：
  - `input-group-text` 与 `.matrix-input/.tool-input` 提升不透明度与文本颜色（深色文字）
  - `category-icon`（仅工具页内，如有）保持高对比圆形浅底

## 具体修改点（按页面）
- `tools/matrix-calculator.html`
  - 给主要容器 `main-content-section` 增加类 `tool-contrast-dark`
  - 保持网格输入区域，提升 `tool-card` 与 `result-card`对比
- `tools/bar-stiffness.html`
  - 同上；确保输入区与结果区卡片均为白底、深边框
- `tools/shape-functions.html`
  - 同上；P1/P2 切换区和结果区卡片对比提升
- `tools/gauss-integration.html`
  - 同上；维度与阶数选择区、结果表卡片对比提升

## CSS新增/调整（集中在 `css/style-new.css`）
- 新增：`.tool-contrast-dark`（限定工具页主内容背景为深色梯度）
- 强化：`.tool-card`、`.result-card`（白底、深边框、强阴影、文本深色）
- 表单：`.input-group-text` 与 `.form-control.matrix-input/.tool-input` 提升对比与焦点态
- 不影响首页与资源页：通过作用域类只在工具页生效

## 缓存与发布
- 更新样式版本号查询参数（如 `style-new.css?v=20251113-3`）避免 CDN/浏览器缓存
- 重新部署到 Vercel 并强制刷新验证

## 验证清单
- 工具页白色卡片在深色背景上清晰可读，标题/正文达到 ≥ 4.5:1 对比度
- 交互控件（下拉、输入、按钮）在深色背景下仍可见焦点态
- 首页与资源页视觉保持原样（无深色背景变更）

请确认该方案，确认后我将回退首页背景改动，并在各工具页面实施对比度优化（不改功能与结构），随后更新样式版本并重新部署。